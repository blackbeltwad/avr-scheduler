I need this maybe for later:
bear -- avr-gcc -mmcu=atmega328p -Os -c *.c

ROUGH IDEA:

POP FLOW: (TOP ----> BOTTOM)  (CPU STATE -----> RETURN ADRESS) AFTER POP: (RETURN ADDRESS) AFTER RETI: ()

PUSH FLOW: (TOP ----> BOTTOM) CURRENT:(RETURN ADDRESS) PUSH:(CPU STATE ---> RETURN ADDRESS)
PTRS->WHICH_STACK = CPU SP LOCATION; PTRS->WHICH_STACK++; CPU SP LOCATION = PTRS->WHICH_STACK
